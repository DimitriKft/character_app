 {% extends 'base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block monTitre %}
Armes {{ weapon.nom }}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-2">
            <img src="{{asset('images/armes/' ~ weapon.nom|replace({'é':'e'})g ~ '.png')}}">
            <h2>{{ weapon.nom}}</h2>
            <h5>Description : {{ weapon.description }}</h5>
            <p>{{ weapon.degat }}</p>
        </div>
    </div>


{% endblock %}
